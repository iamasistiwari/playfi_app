import { View, Text, ActivityIndicator } from "react-native";
import React from "react";

const Loader = () => {
  return <ActivityIndicator size={"small"} color={"#e5e5e5"} />;
};

export default Loader;
